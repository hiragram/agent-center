{
  "name": "agent-center",
  "version": "0.1.1",
  "private": true,
  "description": "Relay Event Emission Protocol events to local agent runs.",
  "type": "module",
  "bin": {
    "agent-center": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "npm run build",
    "test": "npm run build && node --test test/*.test.mjs",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@event-emission-protocol/client": "https://raw.githubusercontent.com/hiragram/agent-center/main/vendor/event-emission-protocol-client-0.1.0.tgz",
    "@event-emission-protocol/core": "https://raw.githubusercontent.com/hiragram/agent-center/main/vendor/event-emission-protocol-core-0.1.0.tgz"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
